SELECT FirstName, LastName, City, State
FROM Person AS p
LEFT JOIN Address AS a
  ON p.PersonID = a.PersonID;
  
  # 1. LEFT JOIN will have the person without address information
  # 2. the join variable doesn't need to be in the output. 
